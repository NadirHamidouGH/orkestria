// Padding and Spacing Constants
/// Default padding used for most layout elements
const defaultPadding = 16.0;

/// Half of the default padding, useful for smaller padding spaces
const paddingHalf = defaultPadding / 2;

/// Smaller padding used for compact UI components or spacing between text
const smallPadding = 4.0;

/// Larger padding used for bigger gaps, especially for layout sections
const largePadding = 32.0;

// Font Sizes Constants
/// Extra small font size, ideal for fine details like small labels or footnotes
const double fontSizeExtraSmall = 10.0;

/// Small font size for minor text such as captions or auxiliary text
const double fontSizeSmall = 12.0;

/// Regular font size for standard body text or secondary content
const double fontSizeRegular = 14.0;

/// Medium font size for slightly more emphasized body text or section headings
const double fontSizeMedium = 16.0;

/// Large font size used for headings or important content
const double fontSizeLarge = 18.0;

/// Extra large font size, typically for main headings or significant titles
const double fontSizeExtraLarge = 24.0;

/// Heading font size used for primary title text
const double fontSizeHeading = 32.0;

// Line Heights Constants
/// Tight line height for minimal spacing, useful for headlines and compact text
const double lineHeightTight = 1.2;

/// Normal line height for most body text and general readability
const double lineHeightNormal = 1.5;

/// Relaxed line height for a more spacious feel, suitable for longer paragraphs
const double lineHeightRelaxed = 1.75;
