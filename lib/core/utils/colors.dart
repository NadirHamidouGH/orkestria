import 'package:flutter/material.dart';

// Colors
/// Primary color used throughout the app (buttons, highlights, etc.)
const primaryColor = Color(0xFF2697FF);

/// Secondary color for complementary elements (icons, links, etc.)
const secondaryColor = Color(0xFF2A2D3E);

/// Background color for main areas of the app
const bgColor = Color(0xFF212332);

/// Alternate primary color for app elements
const kPrimaryColor = Color(0xFF4B5563);

/// Lighter variant of primary color for subtle elements
const kPrimaryLightColor = Color(0xFF374151);